# Phase AM — Tier-2 first-wave field survey (pre-registration)

Registered: 2026-08-04, BEFORE any replay on these instruments.
Charter: D148 tiered-universe expansion + D149 balance-patch roadmap.
These four fields are the ONLY virgin offline data we own (DATA_LEDGER:
banked 2026-08-04, zero prior mining by any lane).

## Question (exploratory, survey-grade)

Which existing weapons produce a plausibly-positive design-region
signal on XAUUSD / XAGUSD / USOIL / USTEC? Output is NOMINATIONS for
chartered follow-up studies — never promotions. Same class as Phase AL.

## Setup

- Data: research repo `data/parquet_tier2/{SYM}_H4.parquet`
  (Dukascopy). Design region **2015-01-01 → 2022-12-31** only;
  **2023+ stays SEALED** (DATA_LEDGER rule 4, pre-sealed before first
  contact — honored here on first contact).
- Engine: product `product` @ `48cf140` (post-I030 symbol-aware pips +
  field-card pip values). Full deployed roster, phi41 arm, symbol
  universes expanded to the four survey fields (same shim as Phase AL).
- **equity=500** (the REAL v2 demo account), declared: the FX-era $100
  sandbox default would strangle metals at min lot ($5 cap vs $3–15
  honest min-lot risk). No imaginary balance — the account exists.
- Field cards: product `company/rd/field_cards/tier2-first-wave.md`.

## Declared distortions (carried, not hidden)

1. Zone-grammar pip thresholds are major-calibrated → price-scale
   filters effectively OFF on these tapes (impulse floors pass
   trivially). Survey is directional only.
2. FX cost model → costs understated. Positive cells must survive an
   explicit cost-honesty pass in their follow-up protocol.
3. regime_fit centering (30 FX pips) tilts high here.
4. USOIL Apr-2020 negative-price episode is in-tape; report flags it.

## Readout (same convention as AL)

Per agent×symbol cells: n, win rate, PF, mean R, total pips.
Nomination floor (same as AL): n ≥ 60 AND PF ≥ 1.15 AND mean R > 0
→ "chartered follow-up candidate"; n in [20, 60) positive → "noted,
capped". Everything else: reported, closed. Multi-start K=5 standard
+ single sealed 2023+ open applies to any follow-up, not the survey.

## Outputs

`results/raw/survey/` tape + `results/survey.json` + REPORT.md.
DATA_LEDGER row added on execution (design window consumed).
